# Progress

## 2026-07-30

- Started Phase 5: ACT-style teleop demonstration capture.
- Decision: RTDE/ROS2 teleop end-effector commands are useful metadata but should not be the primary ACT training action when the policy outputs joint commands.
- Added fixed-rate `qpos_gripper` demo capture:
  - New `qpos_demo_collector.py` builds `observation_t -> action_t` pairs where `action_t` is the next sampled joint/gripper state.
  - New `FixedRateQposDemoEngine` records one delayed frame per timer tick after the first priming sample.
  - Raw `/teleop/command` payloads are preserved under frame metadata as `raw_teleop_command` when present.
  - `collector_node` now supports `sampling_mode:=fixed_rate` and `sample_rate_hz`.
  - Added `data_collection_teleop_demo_qpos.launch.py` for `trainable/policy/qpos_gripper` teleop demonstrations.
- Added regression test `test_qpos_demo_collector.py` covering next-state qpos/gripper actions and raw teleop metadata retention.
- Local pytest is unavailable in this Windows Python environment, so verification used a direct Python runner for the new test plus compile checks.
- Verification passed:
  - direct qpos demo tests: 2 passed
  - `compileall` passed for `data_collection_pkg/data_collection_pkg`
  - `py_compile` passed for `data_collection_teleop_demo_qpos.launch.py` and `setup.py`
  - collector/qpos demo imports passed

## 2026-07-28

- Started Phase 1: source-aware recording.
- Created persistent project plan files.
- Added source-aware recorder tests for HTTP API action source.
- Parameterized `TeleopJsonlRecorder` and `TeleopFrameBuilder` with `source`, `runtime_mode`, `schema_name`, and `action_topic`.
- Parameterized collector config from ROS parameters: `source`, `runtime_mode`, `dataset_schema`, and `action_topic`.
- Fixed custom action topics so collector engine triggers on the configured topic instead of only `/pika/teleop/servo_l_command`.
- Moved HTTP API data-collection action topic default to `/ur5e/control/action_event` in `control_layer`.
- Verification passed:
  - data pipeline tests: 70 passed
  - HTTP API bridge/core tests: 7 passed
  - compile checks passed for `data_collection_pkg` and `control_layer/ur5e_http_api`
- Continued Phase 2:
  - Added standard JSON action event helper for `/ur5e/control/action_event`.
  - Added data pipeline parsing for JSON action events carried by `std_msgs/String.data`.
  - Allowed action event payload metadata to override frame metadata.
  - Changed control-layer HTTP API action publisher to publish JSON `String` events.
  - Connected `/api/control/qpos` and `/api/control/action` paths to `qpos_gripper` action event recording.
  - Verification passed:
    - data pipeline tests: 72 passed
    - HTTP API bridge/core tests: 8 passed
    - compile checks passed for `data_collection_pkg` and `control_layer/ur5e_http_api`
- Changed schema structure:
  - `qpos_gripper` now writes to `trainable/policy/qpos_gripper`.
  - `teleop_servo_l_pose` now writes to `trainable/teleop/servo_l_pose`.
  - `delta_ee_pose` now writes to `trainable/policy/delta_ee_pose`.
  - Added `http_api_action` at `debug/http_api/action`.
- Updated HTTP API action events so top-level `action_schema` is `http_api_action`; original command schema is preserved in action payload.
- Verification passed:
  - data pipeline tests: 74 passed
- Advanced real-robot P0 preparation:
  - Added `/ur5e/control/delta_ee_pose_event` and `/auto_grasp/event` topic constants in the control layer.
  - Added control-layer publishers for HTTP delta end-effector events and auto grasp macro events.
  - Added `data_collection_http_api.launch.py` with separate collectors for HTTP action audit, HTTP delta end-effector data, and auto grasp macro events.
  - Added collector `required_cameras` parameter so debug HTTP collectors can run without mandatory camera gates.
  - Added collector `max_sync_delta_s` parameter support for launch-controlled sync tolerance.
  - Packaged the new HTTP API launch file.
- Verification passed:
  - data pipeline tests: 77 passed
  - HTTP API bridge/core tests: 8 passed
  - compile checks passed for `data_collection_pkg` and `control_layer/ur5e_http_api`
- Migrated control-layer data publishing out of `ros_controller.py`:
  - Added independent `http_api_data_bridge` proxy node.
  - Removed data_collection publishers, parameters, callbacks, and event side effects from `ros_controller.py`.
  - Bridge forwards HTTP API requests to the proven API server, then publishes data events on success.
  - Control path remains responsible only for robot control.
- Verification passed:
  - control-layer bridge/core tests: 11 passed
  - data pipeline full tests: 77 passed
  - compile checks passed for `control_layer/ur5e_http_api`
- Parameterized gripper state topic for Robotiq compatibility:
  - Added `gripper_state_topic` collector parameter.
  - Updated recorder/builder/message adapter to preserve custom gripper topics.
  - Updated HTTP API data collection launch with `gripper_state_topic`.
- Verification passed:
  - data pipeline tests: 80 passed
  - compile check passed for `data_collection_pkg`
  - HTTP API bridge/core tests: 8 passed
  - compile checks passed for `data_collection_pkg` and `control_layer/ur5e_http_api`
- Moved `delta_ee_pose` from `trainable/policy/delta_ee_pose` to `debug/http_api/delta_ee_pose`.
- Verification passed:
  - data pipeline tests: 74 passed
- Started `teleop_ur_ros2` adaptation:
  - Added trainable `teleop_twist_gripper` schema at `trainable/teleop/twist_gripper`.
  - Added `data_collection_teleop_ur_ros2.launch.py` to collect `/teleop/command` string events.
  - Added an independent control-layer `teleop_data_bridge` node that mirrors `/servo_node/delta_twist_cmds` into `/teleop/command` without changing `teleop_ur_node`.
  - Parameterized the bridge gripper state message type for Robotiq deployments.
  - The bridge records action vectors as `[vx, vy, vz, wx, wy, wz, gripper]` with `source=teleop`.
  - Verification passed:
    - full data pipeline tests: 81 passed
    - teleop bridge test: 1 passed
    - teleop collector smoke accepted 1 `/teleop/command` frame
    - compile checks passed for `data_collection_pkg` and `control_layer/teleop_ur_ros2`
- Added read-only hardware interface verification:
  - New `hardware_interface_check` executable in `data_collection_pkg`.
  - Checks `/joint_states`, Robotiq gripper state, `/tf`, `/tf_static`, optional cameras, optional safety state, and optional `base_link -> tool0` TF lookup.
  - Supports gripper message type parameters in both `pkg.msg:Type` and `pkg/msg/Type` forms.
  - Does not publish control commands, call HTTP API, start teleop, or write dataset frames.
- Removed PIKA topic names from data-pipeline defaults:
  - Default teleop action topic is now `/teleop/command`.
  - Default gripper state topic is now `/gripper/state`.
  - Topology monitor and HTTP API launch defaults now use the neutral gripper topic.
  - README and real-robot/manual verification docs now show neutral topic names.
  - Verification passed:
    - data pipeline tests: 81 passed
    - compile check passed for `data_collection_pkg`
- Prepared URSim teleop collection smoke test:
  - Found and fixed a `teleop_ur_node` blocker where `control_loop()` read `self.enable` instead of the declared `self.enabled` state.
  - Added a regression source test for the enabled-state field name.
  - Verification passed:
    - `teleop_ur_ros2` compile check passed
    - direct enabled-state assertion passed
  - Local pytest could not run because this Windows Python environment does not have `pytest` installed.
- Cleared neutral-topic and current teleop path defaults:
  - Data pipeline default teleop schema is now `teleop_twist_gripper`.
  - Current teleop dataset docs use `trainable/teleop/twist_gripper`.
  - Default gripper topic is `/gripper/state` in the teleop collection launch, hardware check, teleop data bridge, and HTTP full-stack launch config.
  - Removed the unused PIKA-named data-pipeline bridge entry point and replaced it with neutral `teleop_bridge_node`.
  - Removed the legacy HTTP bridge helper that emitted `teleop_servo_l_pose`.
  - Verification passed:
    - data pipeline tests: 94 passed
    - teleop bridge/source tests: 3 passed
    - HTTP bridge tests: 8 passed
    - compile checks passed for `data_collection_pkg`, `teleop_ur_ros2`, and `ur5e_http_api`
